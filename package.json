{
  "name": "prahari",
  "version": "1.0.0",
  "private": true,
  "description": "PRAHARI - AI-assisted Crime Intelligence & Analytics Platform for Karnataka State Police",
  "scripts": {
    "test": "jest",
    "clean": "node scripts/clean-build.js",
    "pack:shared": "node scripts/pack-shared.js",
    "pack:shared:only": "node scripts/pack-shared.js --pack-only",
    "serve": "node scripts/clean-build.js && node scripts/pack-shared.js && catalyst serve"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  },
  "dependencies": {
    "uuid": "^8.3.2",
    "zcatalyst-sdk-node": "^3.4.0"
  }
}
