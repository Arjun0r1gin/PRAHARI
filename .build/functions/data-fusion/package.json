{
  "name": "data-fusion",
  "version": "1.0.0",
  "main": "index.js",
  "private": true,
  "description": "PRAHARI Data & Fusion Engine — ingests heterogeneous police CSV feeds, normalises, deduplicates, and persists unified records to Catalyst Data Store.",
  "scripts": {
    "test": "jest --testPathPattern=tests/unit --colors",
    "test:coverage": "jest --testPathPattern=tests/unit --coverage"
  },
  "dependencies": {
    "@prahari/shared": "file:../../vendor/prahari-shared-1.0.0.tgz",
    "csv-parse": "^5.5.5",
    "express": "^4.18.2",
    "uuid": "^9.0.0",
    "zcatalyst-sdk-node": "latest"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  },
  "jest": {
    "testEnvironment": "node",
    "testMatch": [
      "**/tests/unit/**/*.test.js"
    ]
  }
}
